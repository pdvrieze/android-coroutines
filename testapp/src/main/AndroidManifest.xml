<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="nl.adaptivity.android.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="TestActivity1" android:exported="true" android:label="@string/label_ac_callback">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="TestActivity3" android:exported="true" android:label="@string/label_ac_coroutine">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="TestActivity2" />
        <activity android:name="TestActivity4" />
        <activity android:name="TestActivity5" />
        <activity android:name="TestActivity6" />

    </application>
</manifest>
